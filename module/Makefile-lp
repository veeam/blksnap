# SPDX-License-Identifier: GPL-2.0
# Additions for standalone bdevfilter module

EXTRA_CFLAGS += "-DHAVE_LP_FILTER"
# EXTRA_CFLAGS += "-DBDEV_FILTER_LOCKAPI"

bdevfilter-y += lp_filter.o
obj-m	 += bdevfilter.o
